name: Verify pull request

on:
  pull_request:
    branches: [ "main" ]

jobs:
  build:
    name: Verify pull request
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Greet
        run: echo Ciao!
